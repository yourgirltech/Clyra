version: '3.9'

services:
  postgres:
    image: postgres:16-alpine
    container_name: clyra-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: clyra
      POSTGRES_USER: clyra
      POSTGRES_PASSWORD: clyra
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
