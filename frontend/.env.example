# Base URL of the Clyra backend API. No trailing slash.
# Local dev: leave unset (services fall back to http://127.0.0.1:8000).
# Netlify: set VITE_API_BASE to the deployed Render URL, e.g.
#   https://clyra-backend.onrender.com
VITE_API_BASE=http://127.0.0.1:8000
