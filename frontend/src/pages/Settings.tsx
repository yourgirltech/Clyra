export function Settings() {
  return (
    <div className="p-6 md:p-8">
      <div className="mb-6">
        <p className="text-sm font-medium uppercase tracking-[0.2em] text-indigo-600">Configuration</p>
        <h1 className="mt-2 text-3xl font-semibold text-slate-900">Settings</h1>
      </div>

      <div className="rounded-2xl border border-slate-200 bg-slate-50 p-8 text-slate-600 shadow-sm">
        <p className="text-lg font-medium text-slate-700">Settings — coming after the agentic layer lands</p>
        <p className="mt-2 text-sm text-slate-500">
          Workspace preferences, approval controls, and agent guardrail configuration will be added once
          the Phase 4 AI pipeline is in place.
        </p>
      </div>
    </div>
  )
}
