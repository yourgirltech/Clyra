# API Design

## Base URL
The backend is expected to expose a FastAPI application under the local development URL: http://localhost:8000.

## Health
- GET /api/health
- Returns service status with a simple health payload.

## Future endpoints
- Claim detail retrieval
- Rule engine evaluations
- AI recommendation prompts
- Human approval workflow callbacks
